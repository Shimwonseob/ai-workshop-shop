{
  "name": "ai-workshop-shop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply ai-workshop-shop-db --local",
    "db:remote": "wrangler d1 migrations apply ai-workshop-shop-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
