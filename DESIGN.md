# 쇼핑몰 디자인 기준

## 1. 기준 화면과 원칙

새 기준 화면은 `references/screens/home1.png`, `home2.png`, `home3.png`,
`cartegory.png`, `detail.png`, `cart.png` 여섯 장이다. 화면의 로고·브랜드명·고유
카피·회사 정보·실제 상품 사진은 복제하지 않는다. 상품 사진은
`references/products/` 파일만 사용한다.

새 화면에 보이는 배너, 쿠폰, 할인, 배송, 후기, 퀵메뉴, 메가 메뉴는 `SHOP.md`에
데이터와 기능이 있을 때만 사용한다. 없는 정보는 화면 밀도만 참고하고 임의로 채우지
않는다. 외부 이미지·그라데이션·그림자·애니메이션을 새로 만들지 않는다.

## 2. 독립적인 한국형 이커머스 방향

- 상단 정보량과 상품 탐색 밀도는 한국 대형 커머스 수준으로 구성한다.
- 중앙에는 우리 브랜드의 텍스트 영역을 둘 수 있으나 컬리의 로고·서체·표기는 쓰지
  않는다. 우리 브랜드명은 별도 결정값이다.
- 홈은 단일 상품 그리드가 아니라 데이터가 있는 merchandising 섹션을 세로로 쌓는다.
- 추천·할인·베스트·신상품을 기본 슬롯으로 두고, 쿠폰·특가·카테고리 추천은 데이터가
  있을 때 추가한다.
- 상품 이미지와 가격, 상품명, 주요 행동이 장식보다 먼저 보이게 한다.

## 3. 색상 토큰과 역할

| 색상 | UI 역할 | 확인 화면 |
|---|---|---|
| `#5B0081` | 브랜드 기본색·활성 내비게이션·상단 띠 | 여섯 화면 상단 |
| `#641F92` | 핵심 채우기형 버튼 | detail·cart 장바구니/주문 버튼 |
| `#BA75FF` | 쿠폰·프로모션 badge | home1·home2·home3 |
| `#8A4BC6` | 할인율·보조 강조 문자 | detail 가격 영역 |
| `#F5EDFF` | 혜택·프로모션 보조 면 | detail 혜택 칩 |
| `#1B012B` | 메인 프로모션의 어두운 면 | home1 히어로 영역 |
| `#FFFFFF` | 헤더·본문·카드 기본 면 | 공통 |
| `#F3F5F8` | 장바구니 페이지 바탕 | cart |
| `#F7F7F7` | 랭킹 카드·중립 보조 블록 | home3·cart |
| `#EDEFF3` | 수량 조절·합계 보조 면 | cart |
| `#DDDDDD` | 일반 컨트롤·탭 테두리 | home·cart |
| `#E9EBEE` | 행·가격 영역 구분선 | detail·cart |
| `#2C2C2C` | 기본 제목·본문·가격 텍스트 | 새 화면 전반 |
| `#878F9A` | 보조 설명·이전 가격·비활성 정보 | detail·cart |

배너·쿠폰·배송·후기 데이터가 없는 화면에는 그 전용 색을 장식으로 재사용하지 않는다.
사진 속 색과 제품 패키지의 브랜드색은 UI 토큰이 아니다.

## 4. 글꼴과 위계

- 글꼴 이름: 
- 대체 글꼴: 
- 숫자와 한글은 같은 계열로 맞춘다.
- 새 PNG의 캡처 배율을 알 수 없어 정확한 글자 크기·숫자 굵기·줄높이는 확정하지
  않는다.

| 요소 | 위계 | 크기 | 굵기 | 줄높이 |
|---|---|---:|---|---:|
| 페이지 제목 | 장바구니·카테고리 제목 |  | 굵게 |  |
| 홈 섹션 제목 | 추천·할인·베스트·신상품 |  | 중간 |  |
| 상품명 | 카드·상세 핵심 정보 |  | 보통/굵게 |  |
| 한 줄 설명 | 상품명 아래 보조 정보 |  | 보통 |  |
| 정상가 | 할인 전 가격 |  | 보통 |  |
| 할인율·판매가 | 가격 강조 |  | 굵게 |  |
| badge·후기 수 | 카드 보조 정보 |  | 보통/중간 |  |
| 내비게이션 | 카테고리·신상품·베스트·알뜰쇼핑·특가혜택·장바구니 |  | 보통/중간 |  |
| 버튼 | 담기·장바구니·주문 |  | 굵게 |  |
| 상세 정보 | 배송·판매자·포장·단위·중량 |  | 보통 |  |

가격은 통화 단위보다 숫자가 강하게 보인다. 데이터가 있을 때만 정상가 취소선과
할인율을 함께 쓴다.

## 5. 공통 헤더

### 구조

1. 상단 유틸리티 영역
2. 중앙 브랜드 텍스트 영역
3. 큰 검색창
4. 카테고리 탐색
5. 신상품·베스트·알뜰쇼핑·특가혜택
6. 장바구니

`cartegory.png`의 수십 개 항목 메가 메뉴는 참고 전용이다. 실제 분류가 네 개이면
네 개의 단순 탐색 링크만 사용한다. 검색·찜·알림은 `SHOP.md`에서 기능을 확정한 경우에만
활성화한다.

### 치수·상태

- 헤더에는 흰 면과 얇은 `#E9EBEE` 하단선을 사용한다.
- 검색창은 흰 면, `#5B0081` 테두리, 작은 모서리를 사용한다.
- 정확한 행 높이, 콘텐츠 `max-width`, 스크롤 시 고정 여부는 화면 배율만으로 확정하지
  않는다.
- hover, focus, disabled, error, success, animation, 모바일 breakpoint는 미정이다.

## 6. 홈과 상품 카드

홈은 데이터가 있는 섹션만 아래 순서로 표시한다.

1. 메인 프로모션(배너 이미지가 없으면 텍스트와 단색 면만)
2. 오늘의 추천
3. 지금 할인 중
4. 쿠폰 적용 상품
5. 베스트 상품
6. 신상품
7. 카테고리별 추천
8. 이번 주 특가

상품 행은 기본 4열이며 새 화면에서 카드 관찰 폭은 약 `156px`, 카드 간격은 약
`12px`이다. 이 값은 캡처 관찰값이지 공통 CSS `max-width`가 아니다.

카드의 표시 순서는 데이터가 있을 때 다음을 따른다.

1. 1:1 상품 이미지 (`object-fit: contain`)
2. badge
3. 브랜드 또는 분류
4. 상품명
5. 한 줄 설명
6. 정상가
7. 할인율
8. 판매가
9. 쿠폰 라벨
10. 후기 수
11. 장바구니 버튼

값이 없는 항목은 숨긴다. 상품 사진은 `references/products/`에서만 가져오고, 사진을
잘라내지 않는다. 담기 버튼은 흰 면, `#DDDDDD` 1px 선, 작은 모서리를 기본으로 한다.

## 7. 카테고리 페이지

- 페이지 제목: 선택한 카테고리
- 하위 카테고리: 데이터가 있을 때만
- 상품 수
- 정렬: 추천순·신상품순·판매량순·낮은 가격순 중 데이터가 있는 항목
- 4열 상품 그리드
- 홈과 같은 상품 카드 정보 위계

사이드바와 다단 메가 메뉴는 만들지 않는다. `SHOP.md`에 하위 카테고리가 없으면
상위 분류만 표시한다.

## 8. 상품 상세

왼쪽에는 큰 1:1 상품 이미지를 둔다. 오른쪽 정보는 다음 순서다.

badge → 브랜드·배송 정보 → 상품명 → 한 줄 설명 → 정상가 → 할인율 → 판매가 →
쿠폰 안내 → 배송 → 배송비 → 판매자 → 포장 타입 → 판매 단위 → 중량·용량 → 수량
조절 → 총 상품금액 → 장바구니 버튼

데이터가 없는 행은 숨긴다. 하단 탭은 상품설명·상세정보·후기·문의를 지원한다.
후기·문의 데이터가 없으면 빈 상태만 표시한다. 좋아요·알림·판매자 카드·추천 상품은
해당 기능이 추가될 때만 사용한다.

## 9. 장바구니

- 페이지 바탕은 `#F3F5F8`이다.
- 좌측 상품 목록 카드, 우측 주문금액 summary 카드의 2열 구조다.
- 상품 목록에는 전체선택·선택삭제, 이미지, 상품명·설명, 개별 가격, 수량, 삭제,
  상품금액 합계를 둔다.
- summary에는 데이터가 있을 때 상품금액·할인금액·배송비·결제예정금액과 주문 버튼을
  둔다.
- 카드 면은 `#FFFFFF`, 큰 둥근 모서리, 내부 여백을 사용한다.
- 배송비·쿠폰 데이터가 없으면 행을 만들거나 무료배송 문구를 넣지 않는다.

## 10. 모서리·선과 미정값

- 콘텐츠 구분선은 `#E9EBEE` 1px이다.
- 일반 컨트롤 테두리는 `#DDDDDD` 1px이다.
- 큰 카드는 둥근 모서리, 상품 이미지와 작은 버튼은 작은 모서리, 수량 조절은
  `#EDEFF3` 캡슐형 면을 사용한다.
- 정확한 반지름, 글꼴명, 숫자 굵기, 줄높이, 공통 max-width, 고정 내비게이션 동작,
  모바일 폭, 포커스·오류·성공·비활성 색은 화면에서 확정할 수 없어 비워 둔다.
