# 쇼핑몰 디자인 기준

## 1. 문서의 범위와 원칙

- 기준 화면은 `references/screens/home.png`, `detail.png`, `cart.png` 세 장이다.
- 이 문서는 세 화면에서 직접 확인할 수 있는 시각 규칙만 기록한다.
- 참고 사이트의 로고, 브랜드명, 문구, 상품 사진은 가져오지 않는다.
- 실제 상품 이미지는 `references/products/`의 8개 파일만 사용하고,
  상품과 파일의 대응은 `SHOP.md`의 상품 표를 따른다.
- 화면에 없는 장식이나 기능을 추가하지 않는다. 특히 필터 사이드바, 메가 메뉴,
  프로모션 배너, 별점·리뷰, 배송·적립 배지는 만들지 않는다.
- 아래 수치는 원본 PNG의 픽셀을 기준으로 읽었다. 래스터 화면만으로 CSS 원값을
  확정할 수 없는 항목은 비워 두었다.

## 2. 전체 인상

- 한국 온라인 쇼핑몰에서 익숙한 형태인 상단 알림 띠, 로고·검색·사용자 도구,
  가로 카테고리 탐색, 촘촘한 상품 정보 위계를 사용한다.
- 장식보다 상품명, 가격, 수량, 합계, 주요 행동을 먼저 보이게 한다.
- 바탕은 흰색과 매우 옅은 회색으로 나누고 보라색은 브랜드 및 핵심 행동에만 쓴다.
- 카드와 버튼은 과도하게 둥글거나 떠 보이지 않게 한다. 그림자는 사용하지 않는다.

## 3. 색상

각 색상은 구현에서 아래 한 가지 역할로만 사용한다.

| 색상 | 역할 | 읽은 위치 |
|---|---|---|
| `#5B0081` | 브랜드 기본색 | 세 화면의 최상단 알림 띠, 검색창 테두리·아이콘 |
| `#641F92` | 핵심 행동색 | `cart.png`의 로그인 버튼 면 |
| `#8A4BC6` | 보조 강조색 | `detail.png`의 혜택·할인 강조 문자 |
| `#BA75FF` | 프로모션 라벨색 | `home.png` 상품 이미지 위 쿠폰 라벨 면 |
| `#FFFFFF` | 기본 콘텐츠 배경색 | 세 화면의 헤더, 상세 본문, 카드 면 |
| `#F3F5F8` | 페이지 보조 배경색 | `cart.png` 장바구니 페이지 바탕 |
| `#EDEFF3` | 입력·합계 보조 면색 | `cart.png` 수량 조절과 합계 안내 면 |
| `#DDDDDD` | 컨트롤 테두리색 | `home.png` 담기 버튼 테두리 |
| `#E9EBEE` | 구분선색 | `cart.png` 카드 내부 가로 구분선 |
| `#222222` | 기본 본문색 | `cart.png`, `detail.png` 제목·가격·본문 |
| `#333333` | 보조 본문색 | `cart.png` 내비게이션과 항목 텍스트 |
| `#878F9A` | 비활성·보조 정보색 | `cart.png` 이전 가격, 안내 문구, 닫기 아이콘 |

색을 섞어 새 색을 만들지 않는다. 사진 안의 색은 UI 색상 토큰으로 사용하지 않는다.

## 4. 글꼴과 글자 위계

### 공통 글꼴

- 글꼴 이름: 
- 대체 글꼴 목록: 
- 숫자와 한글은 같은 계열을 사용한다.
- 기울임꼴, 자간을 크게 벌린 영문 대문자, 장식용 손글씨체는 사용하지 않는다.

### 화면에서 판독한 크기와 굵기

아래 크기는 원본 화면에서 보이는 CSS 픽셀 크기로 판독한 값이다. 굵기는 이미지로
구분 가능한 시각적 단계만 기록하며 숫자 값은 확정하지 않는다.

| 용도 | 크기 | 시각적 굵기 | 숫자 굵기 |
|---|---:|---|---:|
| 페이지 제목 (`장바구니`) | `28px` | 굵게 | |
| 상품 상세 상품명 | `24px` | 굵게 | |
| 상품 상세 현재 가격 | `24px` | 굵게 | |
| 장바구니 결제예정금액 | `24px` | 굵게 | |
| 홈 섹션 제목 | `18px` | 중간 | |
| 주요 버튼 글자 | `16px` | 굵게 | |
| 장바구니 상품 가격 | `16px` | 굵게 | |
| 상품명·기본 본문 | `14px` | 보통 | |
| 헤더 탐색·검색 입력 | `14px` | 보통 | |
| 보조 설명·메타 정보 | `12px` | 보통 | |
| 최상단 알림·유틸리티 | `11px` | 중간 | |

가격은 통화 단위보다 숫자를 더 굵고 크게 보이게 한다. 취소선 가격과 설명은 본문보다
한 단계 연하게 표시한다. 한 화면 안에서 위 표 이외의 새 글자 크기를 만들지 않는다.

## 5. 여백과 배치

### 공통 헤더

- 최상단 알림 띠 높이: `35px` (`detail.png`, `cart.png`)
- 로고·검색·사용자 도구 행 높이: 약 `90px`
- 가로 탐색 행 높이: 약 `40px`
- 탐색 행 아래 구분선: `1px`
- 검색창 높이: `45px`
- 검색창 안쪽 좌우 여백: `14px`
- 상단 도구 아이콘 사이 간격: 약 `24px`

### 홈 상품 목록

- 한 줄 상품 카드 수: `4개`
- 카드 사이 가로 간격: 약 `12px`
- 상품 이미지와 담기 버튼 사이 간격: `4px`
- 담기 버튼과 상품명 사이 간격: 약 `8px`
- 상품명과 가격 정보 사이 간격: 약 `8px`
- 섹션 사이 세로 여백: 약 `72px`
- 상품 이미지는 정사각형 영역 안에서 `object-fit: contain`으로 표시한다.

### 상품 상세

- 본문 위 여백: 약 `20px`
- 좌우 2열 사이 간격: 약 `49px`
- 왼쪽 상품 이미지 표시 영역: 약 `359px × 460px`
- 상품명 위 보조 정보 간격: 약 `8px`
- 가격 블록 위아래 항목 간격: 약 `4px`
- 쿠폰 버튼 높이: 약 `35px`
- 정보 표 한 행의 위아래 여백: 약 `16px`
- 정보 표 라벨 열 너비: 약 `106px`

### 장바구니

- 페이지 제목 위 여백: 약 `48px`
- 페이지 제목 아래 여백: 약 `44px`
- 좌우 카드 사이 간격: `18px`
- 상품 카드 너비: 약 `530px`
- 결제 카드 너비: 약 `352px`
- 카드 안쪽 여백: `16px`
- 장바구니 항목 사이 세로 간격: 약 `16px`
- 상품 썸네일: `60px × 76px` 안에서 원본 비율 유지
- 수량 조절 높이: 약 `32px`
- 결제 카드의 금액 행 간격: 약 `16px`
- 결제 카드와 핵심 버튼 사이 간격: `16px`

## 6. 선

- 헤더 하단과 정보 행 구분선은 `1px` 실선이다.
- 구분선은 `#E9EBEE`를 쓴다.
- 홈의 담기 버튼과 상세의 쿠폰 버튼은 `1px` 테두리다.
- 일반 컨트롤 테두리는 `#DDDDDD`, 브랜드 행동 테두리는 `#5B0081`을 쓴다.
- 점선, 이중선, 장식용 밑줄은 사용하지 않는다.

## 7. 모서리

| 대상 | 반지름 | 읽은 위치 |
|---|---:|---|
| 검색창 | `6px` | 세 화면 공통 헤더 |
| 홈 상품 이미지 | `2px` | `home.png` 상품 카드 |
| 홈 담기 버튼 | `2px` | `home.png` 상품 카드 |
| 상품 상세 큰 이미지 | `10px` | `detail.png` 왼쪽 이미지 |
| 상품 상세 쿠폰 버튼 | `7px` | `detail.png` 가격 영역 |
| 장바구니 흰 카드 | `16px` | `cart.png` 상품·결제 카드 |
| 장바구니 주요 버튼 | `11px` | `cart.png` 로그인 버튼 |
| 수량 조절 면 | `16px` | `cart.png` 상품 수량 컨트롤 |
| 장바구니 합계 안내 면 | `8px` | `cart.png` 상품 카드 하단 |

완전한 원형은 아이콘 버튼이나 수량 조절의 끝부분에만 사용한다. 카드 전체에 캡슐형
모서리를 적용하지 않는다.

## 8. 화면별 적용 원칙

### 상품 목록

- 참고 화면의 프로모션 배너와 외부 사이트 상품 문구는 가져오지 않는다.
- `SHOP.md`의 네 분류와 8개 상품만 표시한다.
- 상품 카드에는 이미지, 상품명, 가격, 담기 행동만 둔다.
- 할인율, 취소선 가격, 쿠폰, 배송비 표시는 데이터가 없으므로 사용하지 않는다.

### 상품 상세

- 참고 화면의 혜택, 쿠폰, 판매자, 배송, 후기 정보는 가져오지 않는다.
- `SHOP.md`에 있는 이미지, 상품명, 가격, 설명과 수량 선택, 장바구니 담기만 둔다.
- 정보가 적다고 장식이나 임의 문구로 빈 곳을 채우지 않는다.

### 장바구니

- 참고 화면의 배송 그룹, 할인, 쿠폰, 로그인 유도 문구는 가져오지 않는다.
- 상품, 수량, 삭제, 합계와 주문하기 행동만 둔다.
- 좌측 상품 카드와 우측 합계 카드의 2열 구조와 정보 위계만 따른다.

## 9. 화면에서 확정할 수 없어 비워 둔 값

- 글꼴 이름과 대체 글꼴 목록
- 각 글자 위계의 정확한 숫자 `font-weight`
- 데스크톱 콘텐츠 영역의 정확한 `max-width`
- 반응형 전환 너비와 모바일 화면의 여백
- 키보드 포커스 상태의 색·선·두께
- 오류, 성공, 경고 상태의 색
- 비활성 버튼의 글자색과 배경색

이 값들은 제공된 세 장의 정지 화면에는 나타나지 않아 판독할 수 없다. 확정 전에는
임의로 채우거나 구현하지 않는다.
